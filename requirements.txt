# Core
pydantic>=2.0

# Connectors
psycopg2-binary>=2.9
confluent-kafka>=2.3
boto3>=1.34
pyarrow>=15.0
requests>=2.31

# Processing
pyspark>=3.5

# Storage — Iceberg (primary)
# Iceberg Spark runtime is loaded via spark.jars.packages at session init.
# pyiceberg is the Python-native catalog client (optional but useful).
pyiceberg>=0.6

# Scheduler
apscheduler>=3.10

# Dev
pytest>=8.0
pytest-mock>=3.12
ruff>=0.4
